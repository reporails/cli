# Unreleased

### Added

- Tooling: `uv run poe specs_check` validates internal subsystem coverage (declared subsystems exist, each spec is within line-budget, modules colocate under one subpackage); `uv run poe spec_drift` flags potentially stale design docs whose source has been edited more recently
- Tooling: expanded `pytest` marker taxonomy in `pyproject.toml` for granular test selection (lane, cost, subsystem) with new poe tasks `test_fast`, `test_arch`, `test_contracts`, `test_markers`
- Tooling: every `tests/*` test function now carries pytest lane (`unit`/`integration`/`e2e`) + subsystem (`subsys_*`) markers; `check_test_markers.py` enforces tagging on every `qa_fast` run, enabling `pytest -m subsys_caching` and similar slicing
- Tooling: hexagonal platform substrate skeleton bootstrapped at `core/platform/{contract,dto,policy,adapters,runtime,config,observability,utils}` with report-only architecture tests guarding pure-layer purity and adapter boundary (`tests/unit/architecture/`)

### Changed

- Internals: relocated `core/{analytics,bootstrap,config,utils}.py` into the hexagonal platform substrate (`core/platform/{observability,config,utils}/`); no user-facing behavior change
- Internals: relocated `core/{models,results}.py` to `core/platform/dto/` and `core/{applicability,levels}.py` to `core/platform/policy/`; pure-layer files now populated and architecture tests catch any forbidden cross-layer import
- Internals: relocated `core/{api_client,payload,registry,rule_builder}.py` to `core/platform/adapters/` and `core/{engine_helpers,merger}.py` to `core/platform/runtime/`; substrate now covers all eight layers
- Internals: `tests/unit/architecture/test_core_purity.py` and `test_adapter_boundary.py` now run in fail mode; two documented allowlist entries track pending cleanups (impure filesystem-detection helpers and `RulesetMap` location), and any new forbidden cross-layer import blocks the build
- Internals: consolidated caching subsystem into `core/cache/` subpackage (`cache.py` → `cache/__init__.py`, `check_cache.py` → `cache/check_cache.py`, `mapper/map_cache.py` → `cache/map_cache.py`); resolves the long-standing co-location warning where cache files spanned two parent directories
- Internals: consolidated five subsystems into named subpackages — `core/funnel/`, `core/classify/`, `core/heal/`, `core/discovery/`, `core/lint/` — each matching its design-spec boundary; specs/sys/cli/DISCOVERY.md added for the newly-named discovery subsystem
- Internals: extracted `RulesetMap` and supporting dataclasses (`Atom`, `FileRecord`, `ClusterRecord`, `RulesetSummary`, `InlineToken`, `TopicCluster`) from `mapper/mapper.py` to `core/platform/dto/ruleset.py`; adapters now import wire-format types from the pure DTO layer and the adapter-boundary architecture test no longer needs an allowlist entry
- Internals: split filesystem-detection helpers (`detect_features_filesystem`, `resolve_symlinked_files`) out of `core/platform/policy/applicability.py` into `core/discovery/features.py`; `applicability.py` now contains only the pure `get_applicable_rules` decision and the pure-core architecture test allowlist is empty
- Internals: relocated the last nine top-level `core/*.py` files into the substrate — `bundled.py` → `core/platform/config/`, `capability.py` → `core/platform/policy/`, `_torch_blocker.py` → `core/platform/runtime/`, and `init.py`/`download.py`/`updater.py`/`update_check.py`/`self_update.py`/`mcp_install.py` into a new `core/install/` subsystem; dropped the `core.init` re-export façade (all importers now resolve to the actual home module)
- Internals: mapper split (Stage 0) — extracted `@path` inline import expansion (`expand_imports`, `_resolve_import_target`, `_IMPORT_REF_RE`, `_FENCED_BLOCK_RE`, `_NON_EXPANDABLE_EXT`, `_MAX_IMPORT_DEPTH`) from `core/mapper/mapper.py` to a new `core/mapper/imports.py`; behaviour verified bit-for-bit against the pre-split baseline on the corpus samples
- Internals: mapper split (model singleton) — extracted `Models` class + `get_models()` lazy singleton (spaCy `en_core_web_sm` + ONNX `MiniLM-L6-v2`, with parallel `warmup()`) from `core/mapper/mapper.py` to a new `core/mapper/models.py`; consumers in classify and embed now import from the dedicated module
- Internals: mapper split (Stage 4) — extracted specificity annotation (`check_specificity`, `KNOWN_CODE_TOKENS`, `CODE_SHAPE_RE`, `_BACKTICK_RE`, `_ITALIC_RE`, `_BOLD_TERM_RE`, `_BOLD_NEGATION_RE`, `_KNOWN_TOKEN_RE`, `_DOTTED_EXCLUSIONS_NORMALIZED`) from `core/mapper/mapper.py` to a new `core/mapper/annotate.py`; classify-prep and parse-stage references now import from the dedicated module
- Internals: mapper split (Stage 3) — extracted the three-phase charge classifier (`classify_charge`, `_classify_phase1/2/3`, sub-phases 3a-3g, `_strip_md_for_classify`, the corpus-calibrated `_ALL_VERBS` lexicon, conditional/context markers, and ~30 supporting helpers) from `core/mapper/mapper.py` to a new `core/mapper/classify.py` (≈1300 lines); the parse stage's `_classify_content` and `_is_structural` now import `classify_charge` and `_ALL_VERBS` from the dedicated module
- Internals: mapper split (Stages 1+2) — extracted `tokenize` + the markdown-it AST walker (`_md_parser`, `_strip_frontmatter`, `_extract_texts`, `_split_at_softbreaks`, `_determine_format`, atom builders for headings/inline/table-rows/fences), the post-classify hooks (`_split_mixed_charge_atoms`, `_rule_confidence`), and the neutral-atom scanners (`_scan_neutral_for_embedded_markers`, `_scan_charged_for_compound_markers`) from `core/mapper/mapper.py` to a new `core/mapper/parse.py` (≈800 lines)
- Internals: mapper split (Stage 6) — extracted `cluster_topics` + clustering helpers (`_compute_centroid`, `_build_topic_clusters`, `_run_agglomerative_clustering`, `TOPIC_CLUSTER_THRESHOLD`) from `core/mapper/mapper.py` to a new `core/mapper/cluster.py`; orchestration spine imports `cluster_topics` for the in-place int8 dequantisation + AgglomerativeClustering path
- Internals: mapper split (Stage 5) — extracted `_embed_atoms_deduped`, `_embed_file_descriptions`, the `_embed_text` builder, and the `_quantize_int8` helper from `core/mapper/mapper.py` to a new `core/mapper/embed.py`; orchestration spine in `map_ruleset` imports the two callable batch entry points, internal helpers stay private to the new module
- Internals: mapper split (serialize) — extracted `save_ruleset_map`, `load_ruleset_map`, the per-atom JSON helpers (`_atom_to_dict`, `_atom_from_dict`, `_decode_embedding_b64`), and the post-hoc deterministic validators (`MapFinding`, `_validate_atom_schema`, `validate_atoms`, `validate_map`) from `core/mapper/mapper.py` to a new `core/mapper/serialize.py`; daemon, daemon-client, and the `core.mapper` package re-exports now import the load/save entry points directly from `serialize` (no shim re-exports left at `mapper.mapper`)
- Build: tightened `requires-python` to `>=3.12,<3.14`; Python 3.14 ships a `pydantic.v1` introspection regression that breaks `import spacy` (Phase 3 charge classifier dep). The CLI's lexicon fallback covered the failure silently but at reduced accuracy. The pin restores Phase 3 spaCy classification under `uv sync`.
- Internals: mapper split (inspect) — extracted the per-file frontmatter parsers (`_extract_frontmatter_yaml`, `_parse_frontmatter_description`, `_parse_frontmatter_globs`) and the agent-registry matchers (`_load_registry`, `_find_best_registry_match`, `_detect_file_loading`) from `core/mapper/mapper.py` to a new `core/mapper/inspect.py` (~150 lines); `map_ruleset` orchestration imports the three call-site entry points and the rest stay private to the new module
- Internals: mapper split (orchestrator) — renamed `core/mapper/mapper.py` to `core/mapper/pipeline.py` to match the per-stage filename convention (one stage = one file inside `core/mapper/`); callers now import the public surface (`map_ruleset`, `content_hash`, `map_file`) from the `core/mapper` package rather than the internal module path
- Internals: mapper split (Stage 7) — extracted `_build_ruleset_map` from the orchestration spine to a new `core/mapper/assemble.py` (now `build_ruleset_map`, the only public function), giving Stage 7 a dedicated home alongside the other stage modules; the orchestrator imports it via the package-internal path
- Build: pinned the `en_core_web_sm` spaCy model as a direct runtime dependency so `uv sync` installs it alongside spaCy; previously the CLI relied on a runtime auto-download that silently failed under `uv` virtualenvs.
- Internals: removed the legacy "recommended" rules-overlay machinery — `download_recommended`, `is_recommended_installed`, `update_recommended`, `get_latest_recommended_version`, `get_recommended_package_path`, `get_installed_recommended_version`, the `RECOMMENDED_*` constants, the recommended branch of `check_for_updates`, the `recommended` and `recommended_path` fields on `GlobalConfig`/`ProjectConfig`, and the `recommended` key in `ails config set/get/list`. User-installed rule packages remain supported through the generic `packages: [...]` mechanism in `.ails/config.yml` (clone any rule pack into `.ails/packages/<name>/` or `~/.reporails/packages/<name>/` and it is picked up automatically)
- Funnel: Rate-limit CTA now surfaces a "Try again in ~N min." hint when the server returns an accurate `reset_in`, between the limit blurb and the upgrade prompt
- Funnel: CTA and bug-report URLs render as OSC 8 terminal hyperlinks with a short clickable label (`github.com/reporails/cli/issues/new`) instead of dumping the full percent-encoded prefilled URL; falls back to the short label on terminals without hyperlink support
- Funnel: demoted the "Could not parse N response body" and "Server returned N for tier=" stderr warnings to debug logging so they no longer print above the diagnostic report; reworded the `unknown_error` CTA to `Diagnostics server returned HTTP <code>` (honest about what we know without claiming the body is unparseable)
- Display: file rows now annotate duplicates with `(+alias)` labels — symlinked surfaces show the differing path component (e.g. `mintlify (+.claude)`), same-directory content-identical pairs show the alternate filename (e.g. `AGENTS.md (+CLAUDE.md)`)

### Fixed

- API client: outgoing diagnostic requests now carry a `User-Agent: reporails-cli/<version>` header instead of the generic httpx default that some upstream filters were rejecting, causing anonymous-tier requests to intermittently return HTTP 403
- Tests: added `TestOutgoingHeaders` regression coverage asserting `User-Agent` starts with `reporails-cli/` on both anonymous and authenticated requests — verified to fail when the UA header is removed, preventing future silent regressions of the same shape

- Check: `frontmatter_valid_glob` no longer crashes on comma-separated `paths:` values; each entry is now split and validated individually, and invalid glob syntax surfaces as a structured check failure instead of an unhandled exception
- Discovery: skill and rule files that appear under multiple agent surfaces via symlinks (e.g. `.claude/skills/` → `.agents/skills/`) are now collapsed to one canonical entry, eliminating duplicate findings and inflated scoring

### Removed

- CLI: removed the hidden `ails map` command (project topology detector). The underlying per-project detection primitives in `core/discovery/discover.py` (`_detect_classification`, `_detect_commands`, `_detect_meta`, `_detect_paths`) are retained as library helpers; only the user-facing command surface and its dedicated output formatters are gone.
