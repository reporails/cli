.idea
.venv
__pycache__

# Check md files (synced from framework)
checks/*/*.md

docs/research

# Reporails caches (backbone.yml is committed, caches are not)
.reporails/.cache/
# Prevent stray .reporails in subdirectories
*/.reporails/

.env

# Claude Code MCP config (machine-specific paths)
.claude/mcp.json
