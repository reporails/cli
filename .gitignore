.idea
.venv
__pycache__

# Framework docs (symlinked from framework repo)
checks/*/*.md
docs/capability-levels.md

docs/research

# Reporails caches (backbone.yml is committed, caches are not)
.reporails/.cache/
# Prevent stray .reporails in subdirectories
*/.reporails/

.env

# Claude Code (machine-specific)
.claude/mcp.json
.claude/settings.local.json
.mcp.json

# Development internals (functional locally, not public)
docs/specs/
CLAUDE.md
.claude/rules/
.claude/settings.json
.claude/hooks/
.claude/skills/
