# Nested Module

Instructions for the nested submodule.

## Commands

- `make build` — Build the submodule
- `make test` — Run submodule tests

## Constraints

- NEVER modify shared state without locking

## Project Structure

```
# TODO: Add project directory layout
```
