rules:
  # Note: True conflict detection requires semantic analysis
  # These patterns detect potential conflict indicators

  - id: M4-both-always-never
    message: "Both ALWAYS and NEVER present - verify no contradictions"
    severity: INFO
    languages: [generic]
    patterns:
      - pattern-regex: "\\bALWAYS\\b"
      - pattern-regex: "\\bNEVER\\b"
    paths:
      include:
        - "**/CLAUDE.md"
        - "**/.claude/rules/*.md"
