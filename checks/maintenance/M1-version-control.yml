rules:
  # Note: git ls-files check requires custom tooling

  - id: M1-gitignore-claude
    message: "CLAUDE.md should NOT be in .gitignore"
    severity: ERROR
    languages: [generic]
    pattern-regex: "CLAUDE\\.md"
    paths:
      include:
        - "**/.gitignore"
